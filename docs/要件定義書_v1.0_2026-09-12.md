# 積算 AI アシスト（モック）要件定義書

| 項目 | 内容 |
| --- | --- |
| 文書名 | 積算 AI アシスト（モック）要件定義書 |
| 版数 | v1.0 |
| 作成日 | 2026-09-12 |
| 作成者 | 伊東 和成 |
| 対象 | 東京油機工業株式会社様 AI 活用ご相談（2026-08-19 初回ヒアリング議事録）に基づく提案用 UI/UX モック |
| リポジトリ | `mock-tokyo-app` |

> 本書は、この要件定義書のみを入力として **同一のモックアプリを再現できる** ことを目的に記述している。画面文言・モックデータ・型定義・スタイル定数は付録に **全文** を掲載しており、実装時は付録の内容をそのまま使用すること。

---

## 1. 背景と目的

### 1.1 背景
- 東京油機工業株式会社様（油圧装置・小口径削進機メーカー、「ベビーモール工法」の業界団体「ベビーモール協会」事務局）では、役所・設計者から届く下水道設計図面に対し、自社の「積算資料」「参考資料（仕様書）」に照らして **適合機種・概算費用を判定する業務** を中心担当者 1 名が担っている（1 日約 10 件）。
- 判断には土質・管径・現場条件などの経験値が入り、仕様書どおりでない回答（例：客先指定 φ100 だが経験上 φ200 でなければ通らない）が発生するが、その暗黙知は標準仕様に昇格されていない。
- 属人化・技能承継・電話対応負荷が課題。社長は「AI の弟子入り」（現担当者の隣に AI を置き、回答案を出させて担当者が確認する運用）を最優先として希望している。

### 1.2 目的
- 提案するシステムの **UI/UX を関係者が実際に触って体験できるモック** を提供する。
- 議事録に示された 7 テーマと当社提案骨子（フェーズ定義・複数 API 切替・利用上限・セキュリティ）を画面として具体化する。
- バックエンド・AI 接続は実装しない。すべてブラウザ内のモックデータで動作する。

### 1.3 議事録テーマと画面の対応

| № | 議事録テーマ | 位置づけ | 画面 | パス |
| --- | --- | --- | --- | --- |
| 1 | AI の「弟子入り」 | 第 1 フェーズ（最優先） | 積算判定アシスト（一覧・詳細） | `/inquiries`, `/inquiries/[id]` |
| 2 | 判断の標準化・仕様への昇格 | 第 1〜2 フェーズ | 標準化・仕様への昇格 | `/knowledge` |
| 3 | 資料の品質チェック | 第 1 フェーズに同梱 | 資料 品質チェック | `/quality` |
| 4 | 図面の直読（マルチモーダル） | 第 2 フェーズ | 図面直読 | `/drawings` |
| 5 | Web 上での一次回答 | 第 2〜3 フェーズ | Web 一次回答 | `/web-answer` |
| 6 | AI 向け積算資料／データセット整備 | 中長期 | Web 一次回答（右カラム） | `/web-answer` |
| 7 | 現場向け動画マニュアル生成 | 別建て・即実行可（第 0） | 動画マニュアル生成 | `/manuals` |
| — | 単一ツール依存回避・ローカル予備・従量課金上限・セキュリティ | 提案骨子 | モデル接続・利用上限 | `/settings` |
| — | KPI・フェーズ定義 | 提案骨子 | ダッシュボード | `/` |
| — | 現場定着（新人でも操作できる） | 提案骨子（リスク提起：現場定着） | 操作ガイド（システムマニュアル） | `/guide` |
| 1 | 案件資料の入力（図面・柱状図・条件表 → AI が読み取り回答案を生成） | 第 1 フェーズ | 新規案件の登録 | `/inquiries/new` |
| — | データの持たせ方が最大の肝（保管場所・つなぎ込み・古い仕様の除外） | 提案骨子（アーキテクチャ） | 参照データ管理 | `/sources` |

---

## 2. スコープ

### 2.1 対象
- 上記 12 画面（動的ルート含む）の UI 実装。
- 画面内の状態変化（承認／修正／相談、解決済みトグル、採用／保留／却下、主系切替、チャット送信、図面読取デモ、マニュアルジョブ選択、新規案件の登録ウィザード、参照データの除外／確認済み）。
- 新規案件登録時の **回答案生成はモックロジック**（`src/lib/mock-ai.ts`）で行う。管径・土質（礫径）・N値・地下水位・延長・入力形式から機種・適合・信頼度・概算・根拠・注意点を決定論的に生成する。
- 案件レビュー状態の **セッション内共有**（React Context）。サイドバーの「確認待ち件数」バッジがレビュー操作に連動すること。

### 2.2 対象外
- 認証・認可、永続化（DB・localStorage）、API 通信、AI モデル呼び出し。
- PDF/Word 出力、ファイルアップロードの実処理（ボタン・ドロップ領域は表示のみ）。
- ダークモード（ライト固定）。
- モバイル最適化（PC 幅 1280px 以上を主対象。Tailwind のレスポンシブ指定は付録どおり実装するが、モバイルでの表示品質は保証しない）。

---

## 3. 技術要件

| 項目 | 指定 |
| --- | --- |
| フレームワーク | Next.js 16.x（App Router、`src/` ディレクトリ構成、Turbopack） |
| 言語 | TypeScript 5.x（`strict: true`） |
| UI | React 19.x |
| スタイル | Tailwind CSS v4（`@import "tailwindcss"` + `@theme` でカスタムトークン定義） |
| アイコン | `lucide-react` |
| フォント | `next/font/google` の `Noto_Sans_JP`（weight 400/500/700、`variable: "--font-noto-sans-jp"`、`display: "swap"`） |
| Lint | ESLint（`eslint-config-next`） |
| パッケージ管理 | npm |
| import alias | `@/` → `src/` |
| Node.js | 24.x |

### 3.1 スキャフォールドコマンド
```bash
npx create-next-app@latest . --ts --tailwind --eslint --app --src-dir --no-import-alias --use-npm --yes
npm install lucide-react
```
※ `--no-import-alias` を指定しても `tsconfig.json` の `paths` に `"@/*": ["./src/*"]` が設定されることを確認する（未設定なら追加）。

### 3.2 npm scripts
`dev` / `build` / `start` / `lint`（create-next-app 既定のまま）。

### 3.3 完了条件（ビルド）
- `npx tsc --noEmit` エラーなし
- `npm run lint` エラーなし
- `npm run build` 成功（`/inquiries/[id]` のみ Dynamic、他は Static）

---

## 4. ファイル構成

```
.
├── .claude/launch.json          # {"name":"dev","runtimeExecutable":"npm","runtimeArgs":["run","dev"],"port":3000,"autoPort":true}
├── README.md
├── docs/要件定義書_v1.0_2026-09-12.md   # 本書
├── package.json
├── next.config.ts               # 既定（変更なし）
├── postcss.config.mjs           # 既定
├── tsconfig.json                # 既定
└── src/
    ├── app/
    │   ├── globals.css          # 付録 D
    │   ├── layout.tsx           # §5.1
    │   ├── page.tsx             # §7.1 ダッシュボード
    │   ├── inquiries/
    │   │   ├── page.tsx         # §7.2 一覧
    │   │   ├── new/page.tsx     # §7.11 新規案件の登録
    │   │   └── [id]/page.tsx    # §7.3 詳細
    │   ├── quality/page.tsx     # §7.4
    │   ├── knowledge/page.tsx   # §7.5
    │   ├── drawings/page.tsx    # §7.6
    │   ├── web-answer/page.tsx  # §7.7
    │   ├── manuals/page.tsx     # §7.8
    │   ├── settings/page.tsx    # §7.9
    │   ├── guide/page.tsx       # §7.10 操作ガイド
    │   └── sources/page.tsx     # §7.12 参照データ管理
    ├── components/
    │   ├── sidebar.tsx          # §5.2
    │   └── ui.tsx               # §6
    └── lib/
        ├── types.ts             # 付録 A
        ├── mock-data.ts         # 付録 B
        ├── format.ts            # 付録 C
        ├── mock-ai.ts           # 付録 G（回答案生成のモックロジック）
        └── inquiry-store.tsx    # §5.3
```

---

## 5. 全体構成

### 5.1 ルートレイアウト（`src/app/layout.tsx`）
- `<html lang="ja" className={notoSansJP.variable}>`、`<body className="antialiased">`。
- `metadata.title = "積算 AI アシスト｜東京油機工業（モック）"`、`description = "AIの弟子入り：積算判定アシスト・資料品質チェック・標準化提案のモックアプリ"`。
- body 直下を `InquiryProvider` で包み、その中に `div.flex.h-screen` → 左 `Sidebar`、右 `main.flex-1.overflow-y-auto` → `div.mx-auto.max-w-7xl.px-8.py-8` に `children`。
- 外枠を `h-screen` 固定にし、**スクロールは `main` 内で行う**（サイドバーは常に固定表示。ページ内アンカー・sticky 目次は `main` を基準に動作する）。

### 5.2 サイドバー（`src/components/sidebar.tsx`、Client Component）
- 幅 `w-64`、高さ `h-screen`、白背景、右ボーダー、縦 flex。
- **ヘッダー**：`Bot` アイコン（navy-700 背景・白・`h-9 w-9 rounded-lg`）＋「積算 AI アシスト」（`text-sm font-bold`）／「東京油機工業 ／ ベビーモール協会」（`text-[11px] text-slate-500`）。
- **ナビゲーション**（順序・ラベル・アイコン・フェーズ表記は固定）：

| href | ラベル | lucide アイコン | 右端表示 |
| --- | --- | --- | --- |
| `/` | ダッシュボード | `LayoutDashboard` | なし |
| `/inquiries` | 積算判定アシスト | `Inbox` | **確認待ち件数バッジ**（`pending_review` 件数 > 0 のとき、amber-500 背景・白文字・丸） |
| `/quality` | 資料 品質チェック | `ClipboardCheck` | 「第1」 |
| `/knowledge` | 標準化・仕様への昇格 | `BookOpenCheck` | 「第1-2」 |
| `/sources` | 参照データ管理 | `Database` | 「第1」 |
| `/drawings` | 図面直読 | `ScanSearch` | 「第2」 |
| `/web-answer` | Web 一次回答 | `Globe` | 「第3」 |
| `/manuals` | 動画マニュアル生成 | `Video` | 「第0」 |
| `/settings` | モデル接続・利用上限 | `Settings` | なし |
| `/guide` | 操作ガイド | `BookOpen` | なし |

- アクティブ判定：`/` は完全一致、それ以外は `pathname.startsWith(href)`。アクティブ時 `bg-navy-50 font-semibold text-navy-800`、アイコン `text-navy-700`。非アクティブ時 `text-slate-700 hover:bg-slate-100`、アイコン `text-slate-400`。
- **フッター**：丸アバター「A」＋「積算担当 A」／「師匠（レビュアー）」、その下に注記「※ モックアプリ：機種・数値・自治体名はすべて架空です」（`text-[10px]`、slate-50 背景）。

### 5.3 状態管理（`src/lib/inquiry-store.tsx`、Client）
- `InquiryProvider`：`useState<Inquiry[]>(seed)`（seed は付録 B の `inquiries`）。
- 提供する API（`useInquiries()`）：

| 関数 | 挙動 |
| --- | --- |
| `approve(id, {note})` | `status: "approved"`, `reviewer: "積算担当 A"`, `reviewerNote: note || "AI回答案どおりでOK。"` |
| `revise(id, {note, learnedDelta})` | `status: "revised"`, `reviewer`, `reviewerNote: note`, `learnedDelta` |
| `escalate(id, {note})` | `status: "escalated"`, `reviewer`, `reviewerNote: note` |
| `reopen(id)` | `status: "pending_review"`、`reviewer`/`reviewerNote`/`learnedDelta` を `undefined` |
| `addInquiry(input: NewInquiryInput)` | `buildInquiry(input, inquiries)`（付録 G）で `Inquiry` を生成し **配列の先頭** に追加、生成した `Inquiry` を返す。id は `INQ-YYYY-MMDD-NN`（当日連番）、`receivedAt` は現在時刻、`status: "pending_review"` |

- Provider 外で `useInquiries` を呼んだ場合は `Error("useInquiries must be used within InquiryProvider")`。
- 永続化しない（リロードで seed に戻る）。

### 5.4 デザイントークン（`globals.css`、付録 D）
- カスタム色 `navy`：50 `#eef2f8` / 100 `#d9e1ef` / 200 `#b3c3df` / 300 `#7f9ac5` / 500 `#3b5a8c` / 600 `#2c4772` / 700 `#1f3559` / 800 `#172845` / 900 `#0f1b30`。
- ページ背景 `#f4f6fa`、文字色 `#0f172a`、`color-scheme: light`、`font-feature-settings: "palt"`。
- その他の色は Tailwind 既定（slate / emerald / sky / amber / rose）。

---

## 6. 共通 UI コンポーネント（`src/components/ui.tsx`）

すべて Server/Client 両用の純粋な表示コンポーネント（`"use client"` 不要）。

| コンポーネント | Props | 仕様 |
| --- | --- | --- |
| `Card` | `children, className?, title?, action?` | `rounded-xl border border-slate-200 bg-white shadow-sm`。`title` または `action` があるとき `header`（`px-5 py-3`、下ボーダー、`h2.text-sm.font-semibold`）を表示。本文は `div.p-5`。 |
| `PageHeader` | `eyebrow?, title, description?, phase?, actions?` | `mb-6 flex flex-wrap items-end justify-between gap-4`。eyebrow は `text-xs text-slate-500`、title は `text-2xl font-bold`、phase は `PhaseTag`、description は `max-w-3xl text-sm text-slate-600`。 |
| `PhaseTag` | `label` | `rounded-full border border-navy-200 bg-navy-50 text-xs font-semibold text-navy-700` |
| `StatusBadge` | `status: InquiryStatus` | ラベルは `statusLabel`（付録 C）。色：pending_review=amber、approved=emerald、revised=sky、escalated=rose、draft=slate（各 `bg-*-50 text-*-800 border-*-200`）。 |
| `FitBadge` | `fit: Fit, size?: "md"\|"lg"` | 正方形（md: `h-7 w-7 text-sm`、lg: `h-12 w-12 text-2xl`）、`rounded-lg font-bold`。◎=emerald-600、○=sky-600、△=amber-500、×=rose-600、文字白。 |
| `SeverityBadge` | `severity` | 「重要度 高/中/低」。高=rose-100/800、中=amber-100/800、低=slate-100/700。 |
| `Chip` | `children, tone?: slate\|navy\|green\|amber\|rose` | `rounded-md px-2 py-0.5 text-xs font-medium`。 |
| `Confidence` | `value: 0-100` | 幅 `w-24` の細バー＋「NN%」。≥85 emerald-500、≥70 sky-500、それ未満 amber-500。 |
| `StatCard` | `label, value, sub?, icon?, tone?: navy\|green\|amber\|sky` | 白カード。label（`text-xs`）＋右上アイコン（tone 色の `rounded-lg p-1.5`）、value `text-3xl font-bold tabular-nums`、sub `text-xs text-slate-500`。 |
| `Button` | `variant?: primary\|secondary\|danger\|ghost` + button 属性 | `rounded-lg px-3.5 py-2 text-sm font-medium`。primary=navy-700 背景白文字（hover navy-800、disabled slate-300）、secondary=白＋slate-300 枠、danger=白＋rose-300 枠 rose-700 文字、ghost=枠なし slate-600。 |
| `KeyValue` | `items: {k, v}[]` | 1 列の `dl`。各行 `flex justify-between`、下ボーダー slate-100、`dt` slate-500、`dd` 右寄せ `font-medium`。 |

---

## 7. 画面要件

共通：各ページは `PageHeader` から始まる。`useInquiries` を使う画面は `"use client"`。金額は `yen()`（付録 C）で「￥4,860,000」形式、日時は `dateTime()` で「09/12 09:42」形式（Asia/Tokyo）。

### 7.1 ダッシュボード（`/`）
**PageHeader**
- eyebrow「2026-09-12（土）」、title「ダッシュボード」
- description「AIが回答案を出し、担当者が「これでよい」と確認する運用。担当者の判断は学習キューへ蓄積され、翌年度の積算資料・仕様書づくりに反映されます。」
- actions：リンク「はじめての方は操作ガイドへ」（`BookOpen` アイコン、`/guide`、白背景・navy-200 枠・navy-700 文字）

**KPI 行**（`grid-cols-2 lg:grid-cols-4`、`StatCard` × 4）
1. 「本日の問い合わせ」= `dashboardStats.todayInquiries`(10)、sub「うち 事前相談 3件」、icon `Inbox`
2. 「AI回答案 確認待ち」= 現在の `pending_review` 件数（動的）、sub「担当者の確認が必要」、icon `Clock`、tone amber
3. 「修正なし承認率（30日）」= `78%`、sub「AI回答案がそのまま採用された割合」、icon `ThumbsUp`、tone green
4. 「一次回答までの平均時間」= `4分`、sub「従来 35分 → 電話待ちを解消」、icon `GraduationCap`、tone sky

**2 段目**（`lg:grid-cols-3`）
- 左 2 列 `Card` title「確認待ちの AI 回答案」、action にリンク「すべて見る →」（`/inquiries`）。`pending_review` の案件を `FitBadge`／案件名／「{client} ・ {受付日時} 受付 ・ {source}」／右側に機種名（「（」より前）と `Confidence`／`StatusBadge` の行で列挙（各行は詳細へのリンク）。0 件時は「確認待ちの回答案はありません。」
- 右列（縦 2 カード）
  - 「弟子（AI）の学習状況」：「学習キュー（担当者の修正差分）」= `learnedDelta` を持つ案件数 + `learningQueue`(5) − 1 件、「標準仕様への昇格提案」= `knowledgeCandidates` のうち 提案中＋採用予定 の件数（`/knowledge` へリンク）、「資料の未解決指摘」= `qualityIssues` の未解決件数（`/quality` へリンク、rose 文字）
  - 「今月の AI 利用コスト」：`yen(monthlyUsed)`(￥18,240)、「上限 ￥50,000」、進捗バー（navy-600）、注記「従量課金の高騰を防ぐため、月額上限と利用者別上限を設定しています」（`Wallet` アイコン）

**3 段目** `Card` title「フェーズ定義（理想像を描いた上で、今回はこのパーツ）」：`phases`（付録 B）を `md:grid-cols-2 xl:grid-cols-4` のリンクカードで表示。各カード：「{no}フェーズ」（navy 太字）＋ `Chip`（稼働中=green／プレビュー=amber／構想=slate）、name（太字）、scope、末尾「期間 {period} ／ 投資 {budget}」。hover で `border-navy-300 bg-navy-50/40`。

### 7.2 積算判定アシスト 一覧（`/inquiries`）
**PageHeader**：eyebrow「AIの弟子入り」、title「積算判定アシスト」、phase「第1フェーズ」、description「届いた図面・条件に対して、AIが積算資料・仕様書・過去の判断履歴を照らし合わせて回答案を作成します。担当者は確認・修正・相談のいずれかで処理してください。」、actions = primary ボタン「新規案件を登録（資料をアップロード）」（`Plus`、`/inquiries/new` へのリンク）。

**参照データの帯**（PageHeader 直下、`border-navy-100 bg-navy-50/60` の横長バー）：「🗄 AI が参照中のデータ」＋「積算資料 2026年版（48p）」「参考資料（仕様書）2026年版（32p）」「判断履歴 412件（2023-04 以降）」「施工実績 186件」＋右端リンク「参照データ管理へ」（`/sources`）。

**フィルタ行**
- セグメントボタン：すべて／確認待ち／承認済み／修正して回答／相談中（各件数を薄色で併記、選択中は白背景＋影）。
- 検索ボックス（`Search` アイコン、placeholder「案件名・依頼元・受付番号」、`title client id` の部分一致・大文字小文字無視）。

**テーブル**（`min-w-[900px]`、横スクロール可）
列：受付（日時＋source の 2 行）／案件（title は詳細へのリンク、下に「{client} ・ {id}」）／条件（「φ{径} ／ {延長}m」＋改行＋土質）／AI 提案機種／適合（`FitBadge`）／信頼度（`Confidence`）／概算（右寄せ `yen`）／状態（`StatusBadge`）。0 件時「該当する案件がありません。」

### 7.3 積算判定アシスト 詳細（`/inquiries/[id]`）
- `useParams` で id を取得し `useInquiries().inquiries` から検索。未存在時「案件が見つかりません。」＋「一覧へ戻る」リンク。
- 上部に「← 一覧へ戻る」リンク。`PageHeader`：eyebrow「{id} ・ {受付日時} 受付 ・ {source}」、title = 案件名、actions = `StatusBadge`。

**レイアウト** `lg:grid-cols-3`
- **左列**
  - `Card`「図面・現場条件（入力）」：`KeyValue` で 依頼元／区分／工種／管径（`φ{n} mm`）／推進延長（`{n} m`）／土質／N値／地下水位／立坑。`source === "手書き図面"` のとき amber 注意枠「手書き図面からの読み取りです。管底高・立坑位置は目視で再確認してください。」
  - `Card`「参照した資料」：`references` を列挙。`doc === "判断履歴"` は `GraduationCap`、それ以外 `BookOpen` アイコン。label（太字）＋「{doc} ・ {page}」。
- **右 2 列**
  - `Card` title「✨ AI 回答案（弟子の提案）」（`Sparkles` アイコン）、action = `Confidence(confidence)`
    - 上段：`FitBadge lg`、「提案機種」＋ machine（`text-lg font-bold`）、右に 概算費用（yen）／日進量（`{n} m/日`）／想定工期（`{n} 日`）の 3 列。
    - `deviationFromSpec` があるとき amber 枠「仕様書との差分（経験値に基づく判断）」＋本文＋「→ この差分は「標準化・仕様への昇格」候補として集計されています。候補を見る」（`/knowledge` リンク）。
    - 下段 2 列：「判断の根拠」（番号丸付きリスト）／「注意点・確認事項」（`AlertTriangle` amber、空なら「特記事項なし」）。
  - `Card`「担当者の確認（師匠のチェック）」— 状態別に 3 表示
    1. **未処理以外**（approved/revised/escalated）：`StatusBadge`＋reviewer、`reviewerNote`（`whitespace-pre-wrap`）。`learnedDelta` があれば sky 枠「学習キューに登録された差分」＋本文。ボタン「回答書を出力（PDF）」（secondary、ダミー）／「確認待ちに戻す」（ghost → `reopen`）。
    2. **確認待ち・モード未選択**：説明文「AI回答案を確認し、処理を選択してください。修正した内容は学習キューに入り、次回以降の回答案に反映されます。」＋ボタン「これでよい（承認）」（primary, `Check`）／「修正して回答」（secondary, `PencilLine`）／「現場経験者・協会へ相談」（danger, `MessageSquareWarning`）。
    3. **モード選択後**：`Chip`（承認=green／修正して回答=navy／相談=rose）＋「積算担当 A として処理します」。textarea（3 行、ラベルは approve「コメント（任意）」／revise「修正内容・依頼元への回答」／escalate「相談内容・相談先」、placeholder は approve「例：AI回答案どおりでOK。」／revise「例：地下水位が高いため薬液注入を必須として回答。工期 +2日。」／escalate「例：礫径 80mm は判断が割れるため、協会 技術委員へ相談。」）。revise のみ追加入力「弟子に教える判断ルール（学習キューへ登録）」（`GraduationCap` sky、placeholder「例：条件 A × 条件 B のとき → 判断 C とする」、sky 枠）。「確定」（approve 以外は本文空欄で disabled）／「キャンセル」。確定後はモード・入力をリセット。

### 7.4 資料 品質チェック（`/quality`）
- ローカル state：`issues`（seed = `qualityIssues`）、`scanning`、`tab: "open"|"resolved"`。
- **PageHeader**：eyebrow「資料の品質担保」、title「積算資料 品質チェック」、phase「第1フェーズ」、description「積算資料・参考資料（仕様書）を AI が横断的に読み、数値の抜け漏れ・外れ値・ページ間の不整合・改定漏れを具体的な根拠つきで指摘します。「この数字があった方がよい」で終わらず、修正案まで提示します。」、actions = 「再チェック」ボタン（`RefreshCw`、押下で 1.5 秒間「チェック中…」＋アイコン回転、disabled）。
- **集計カード** 4 枚（抜け漏れ／外れ値／不整合／改定漏れ、未解決件数）。`Chip` の tone：抜け漏れ=rose、外れ値=amber、不整合=navy、改定漏れ=slate。
- **タブ**「未解決／解決済み」（件数併記）＋注記「対象：積算資料 2026年版（48p）／ 参考資料（仕様書）（32p）・最終チェック 2026-09-12 08:00」（`ScanLine`）。
- **指摘カード**：左にトグルアイコン（未解決 `CircleDashed`、解決済 `CheckCircle2` emerald。クリックで `resolved` 反転）、`Chip(type)`＋`SeverityBadge`＋「{doc} ・ {location}」、description（太字）、slate-50 枠「AI の修正案」＋suggestion。右にボタン「該当ページを開く」（secondary、ダミー）、未解決時のみ「修正済みにする」（ghost → トグル）。解決済みカードは `opacity-70`。0 件時「該当する指摘はありません。」

### 7.5 標準化・仕様への昇格（`/knowledge`）
- ローカル state：`items`（seed = `knowledgeCandidates`）。`setStatus(id, status)`。
- **PageHeader**：eyebrow「暗黙知の資産化」、title「判断の標準化・仕様への昇格」、phase「第1〜2フェーズ」、description「担当者の経験に基づく「仕様書との差分（ぶれ）」を AI が束ね、「これは標準仕様に入れてよいのでは」と提案します。採用したものは翌年度の積算資料・仕様書の改定案として出力できます。」、actions = 「2027年版 改定案を出力（{採用予定件数}件）」（secondary, `FileDown`、ダミー）。
- **集計 3 カード**：「担当者の判断（学習済み）」412件（`GraduationCap` sky）／「束ねた例外パターン」= items 件数（`BookOpenCheck` amber）／「標準仕様へ昇格予定」= 採用予定件数（`ArrowRight` emerald）。
- **候補カード**：`Chip(status)`（提案中=amber／採用予定=green／保留=slate／却下=rose）＋id、pattern（`text-base font-bold`）、「発生 **{n}件** ・ {period} ・ 反映先：{targetDoc}」。右にボタン：現在の status 以外の「採用する」（primary）／「保留」（secondary）／「却下」（ghost）を表示。
  - 3 カラム比較（`md:grid-cols-[1fr_auto_1fr]`）：「現在の仕様書」（slate-50）→ 矢印 →「実際の判断（担当者の経験値）」（sky-50）。
  - emerald 枠「AI の昇格提案」＋suggestion。
  - `<details>`「根拠データ（{n}件）」で evidence をリスト表示。

### 7.6 図面直読（`/drawings`）
- ローカル state：`stage: "idle"|"reading"|"done"`。「サンプル図面で試す」で reading → 1.8 秒後 done。
- **PageHeader**：eyebrow「マルチモーダル」、title「図面直読」、phase「第2フェーズ（プレビュー）」、description「設計図面（PDF・スキャン・手書き）と地盤データを見せるだけで、資料を見せずに「これがいけます」という機種提案まで到達することを目指します。読み取り値には信頼度を付与し、低いものは担当者の確認を促します。」
- **左** `Card`「図面・地盤データのアップロード」：破線ドロップ領域（idle: `FileUp` アイコン＋「平面図・縦断図・柱状図をドロップ」、reading: `Loader2` 回転＋「図面を読み取っています…」、done: 「読み取り完了：sample_plan_no3-no4.pdf」）、「PDF / JPG / PNG / DXF（最大 50MB）」、ボタン「サンプル図面で試す」（`ScanSearch`）、done 時「クリア」。下に「読み取り対象」リスト（平面図：管径・延長・立坑位置・人孔番号／縦断図：管底高・勾配・土被り／柱状図（地質調査）：土質区分・N値・地下水位・礫径／手書き図面：判読できない箇所は信頼度を下げて担当者へ確認依頼）。
- **右上** `Card`「読み取り結果」：done 以外は「図面をアップロードすると、抽出した条件がここに表示されます。」。done 時に以下を行表示（項目／値／`Confidence`。conf < 80 の値は amber 文字）。

| 項目 | 値 | 信頼度 |
| --- | --- | --- |
| 管径 | φ250 | 98 |
| 推進延長 | 62.0 m（No.3 → No.4） | 96 |
| 土質（柱状図） | 砂質土 → 礫混じり砂（GL-4.0m 以深） | 88 |
| N値 | 12 → 25 | 90 |
| 地下水位 | GL-2.6 m | 93 |
| 発進立坑 | φ2.5 m（ライナープレート） | 85 |
| 到達立坑 | φ2.0 m | 82 |
| 管底高（発進） | TP+3.21 m | 71 |

- **右下**（done 時のみ）`Card`「✨ 機種提案（資料を見せずに到達）」：`FitBadge "○" lg`、「BM-R型 礫対応削進機」、「GL-4.0m 以深の礫混じり砂を考慮し、BM-M型ではなく BM-R型 を選定」、概算 ￥6,380,000。`Chip amber`「管底高の信頼度 71% → 要確認」「延長 62m → 中間ジャッキ要否を確認」。ボタン「積算判定アシストへ送る」（primary）／「読み取り値を修正」（secondary）。いずれもダミー。

### 7.7 Web 一次回答（`/web-answer`）
- ローカル state：`msgs`（初期 2 件）、`input`、`typing`。
- **PageHeader**：eyebrow「売上寄与」、title「Web 一次回答」、phase「第3フェーズ（構想）」、description「着手前の「これはできますか？」という気軽な相談に、協会サイト上で AI が一次回答します。「これならできる」と分かれば、やってみようという設計者・工事会社が増え、案件の入口を広げます。」
- **左 2 列** チャットカード：上部バー（`Globe`＋`babymole-assoc.example.jp/consult`（等幅）＋`Chip green`「公開プレビュー」）、高さ 460px のスクロール領域、user は右寄せ navy-700 吹き出し＋`User` アバター、ai は左 `Bot` アバター（navy-700 丸）＋slate-100 吹き出し（`whitespace-pre-wrap`）。typing 中は「回答を作成しています…」。入力行（placeholder「例：礫混じりの地盤で φ150 は施工できますか？」、Enter 送信・IME 変換中は無視、「送信」ボタン `Send`）。注記「※ AI による一次回答です。確定判断は協会担当者が行います。個人情報・図面は入力しないでください。」
  - 送信後 1.2 秒で自動応答：入力に「礫」を含む場合は `canned["礫"]`、それ以外 `canned.default`（本文は付録 E）。
- **右列**
  - `Card`「公開にあたってのルール」：`ShieldCheck` emerald 付き 4 項目（回答は「目安」に限定し、確定積算は担当者確認へ誘導／1 IP あたり 1日 20問の上限（従量課金の高騰防止）／公開ナレッジは社内版と分離（未公開の判断履歴は参照しない）／協会内の合意プロセスを経て公開範囲を決定）。
  - `Card`「🗄 AI 向け積算データセット」（`Database`）：説明文「人間が読む冊子とは別に、他社 CAD 搭載 AI やクローラーが読むための構造化データ（機種・適用範囲・歩掛）を整備します。大手 CAD 上で比較選定される際に自社機械が選ばれるための基盤です。」、黒背景の JSON 例、ボタン「データセットの公開設定」（secondary、全幅、ダミー）。

### 7.8 動画マニュアル生成（`/manuals`）
- ローカル state：`selected`（初期 = `manualJobs[0]`）。
- **PageHeader**：eyebrow「現場の技能承継」、title「動画→作業マニュアル自動生成」、phase「第0フェーズ（先行）」、description「作業動画をアップロードすると、AI が手順を切り出して作業手順書を自動生成します。作業員の引継ぎ・新人教育に使用でき、現場要員の入れ替わりで浅くなった経験を補います。撮影はスマートフォンで可。」、actions = 「動画をアップロード」（primary, `Upload`、ダミー）。
- **左** `Card`「生成ジョブ」：`manualJobs` をボタンで列挙（title＋`Chip(status)`（生成済み=green／生成中=amber／未着手=slate）、「{videoLength} ・ {uploadedAt}」、生成中は amber 進捗バー）。選択中は `border-navy-300 bg-navy-50/50`。
- **右 2 列** `Card` title = 選択ジョブ名、生成済み時 action に「PDF」「Word」ボタン（secondary, `FileDown`、ダミー）。
  - 16:9 の黒プレースホルダ（`Clapperboard`＋「動画プレビュー（{videoLength}）」）。
  - 生成済み：手順を番号丸（navy-700）＋title＋右に「▶ {timestamp}」＋body で列挙。
  - 生成中：`Loader2` amber 回転＋「手順を切り出しています… {progress}%」＋「音声の文字起こし → 場面分割 → 手順文の生成」。
  - 未着手：「アップロード済み。生成を開始してください。」＋「生成を開始」ボタン（ダミー）。

### 7.9 モデル接続・利用上限（`/settings`）
- ローカル state：`primary`（初期 `"claude"`）。
- **PageHeader**：eyebrow「単一ツール依存を避ける」、title「モデル接続・利用上限」、description「自社側の Web アプリを窓口に、裏側で複数の AI API に接続します。接続先を切り替えられるため、モデルの提供終了・通信障害時のバックアップになります。ナレッジは特定ツールに閉じさせず、自社の資産として保持します。」
- **左 2 列** `Card`「接続先モデル（切替可能）」：`modelProviders` を行表示。name＋`Chip(role)`（主系=green／副系=navy／緊急予備（ローカル）=amber。表示ロジック：`primary` と一致すれば「主系」、元 role が主系で非選択なら「副系」、それ以外は元 role）＋`Chip(status)`（稼働中=green／待機=slate／障害=rose）、vendor、note。右に「応答 {秒}秒」（`Activity`）／「今月 {yen}」。ボタン「主系」（選択中、secondary・disabled）または「主系に切替」（primary, `ArrowLeftRight`）。選択行は `border-navy-300 bg-navy-50/40`。下に slate-50 注記「二段構えの運用」＋「基本はフロンティアモデル（Claude／GPT）、外部 API 断時のみローカル小型モデルへ自動フォールバック。ローカルは「ぎりぎり受け答えできる予備」であり、精度は限定的です。」
- **右列**
  - `Card`「🗄 ナレッジの保管」：積算資料・仕様書＝自社ストレージ／判断履歴（412件）＝自社 DB／学習キュー＝自社 DB／モデル側への学習＝「なし（データ非送信）」（emerald）。注記「データベース役と生成役を分離。モデルを切り替えてもナレッジは失われません。」
  - `Card`「🔑 セキュリティ」（`KeyRound`）：`ShieldAlert` emerald 付き 3 項目（API キーはサーバー側で保管・90日ごとに更新／依頼元の個人情報はマスクして送信／社内版と Web 公開版のナレッジを分離）。
- **下段** `Card`「利用上限（従量課金の高騰防止）」：「今月の合計」`yen(18,240)` / 上限 `yen(50,000)`、「上限を変更」（secondary、ダミー）。テーブル（利用者／利用状況バー＋%／今月／上限）。バーは使用率 > 80% で rose-500、それ以外 navy-600。

| 利用者 | 上限 | 今月 |
| --- | --- | --- |
| 積算担当 A | 20,000 | 11,800 |
| 積算担当 B（後継） | 15,000 | 5,240 |
| 重盛社長 | 10,000 | 1,200 |
| 協会 技術委員 | 5,000 | 0 |

### 7.10 操作ガイド／システムマニュアル（`/guide`）
- Server Component（state なし）。新人社員が読むことを想定し、**専門用語を避けた平易な日本語**で記述する。
- **PageHeader**：eyebrow「はじめての方へ」、title「操作ガイド（システムマニュアル）」、description「このシステムの目的・画面の見方・毎日の使い方を、新人の方でも迷わないようにまとめています。まず「1日の基本の流れ」だけ読めば使い始められます。」
- **レイアウト** `lg:grid-cols-[220px_1fr]`：左に sticky 目次（`lg:sticky lg:top-6 lg:self-start`、`Card` 内に「目次」＋アンカーリンク 7 件）、右に本文セクション（`space-y-10`）。各セクションは `id` 付き `section.scroll-mt-6`、見出しは `border-l-4 border-navy-700` の左罫線付き `text-lg font-bold`。
- **セクション構成と内容**

| id | 見出し | 内容 |
| --- | --- | --- |
| `intro` | 1. このシステムでできること | 概要文（「AI が下書き（回答案）を作り、担当者が確認する」を強調）＋ 3 ステップカード（① AI が回答案を作る／② 担当者が確認する／③ AI が育つ）＋ Tip「大切な考え方：AI は「弟子」、担当者は「師匠」です。…遠慮なく修正してください。」 |
| `screen` | 2. 画面の見方 | メニュー 8 項目の説明リスト（アイコン＋画面名リンク＋一言説明。「ふだん使うのは上の 2 つだけで十分」）。カード「よく出てくるマークの意味」：`FitBadge` ◎○△× の意味 4 行、`StatusBadge` 4 種の意味 4 行。 |
| `daily` | 3. 1日の基本の流れ | 番号丸付き 5 ステップ（朝：ダッシュボードを開く／確認待ちの案件を上から順に開く／AI 回答案を読む／3 つのボタンから 1 つ選ぶ／確認待ちがゼロになったら終了）＋ amber Tip「信頼度が 70% 未満（オレンジ）の案件、黄色い「仕様書との差分」枠がある案件は、いつもより慎重に確認してください。」 |
| `inquiries` | 4. 積算判定アシストの使い方 | カード「一覧画面（/inquiries）」（フィルタ・検索・案件名クリックの 3 点）。カード「詳細画面」：3 色の操作カード（承認=emerald／修正して回答=sky／相談=rose。各操作の意味と手順）、「判断ルールの書き方の例」2 件（礫径 20mm 超 × φ100 指定 → φ200（BM-R型）を提案／粘性土 × 地下水位が管頂より 1.0m 以上高い → 薬液注入を必須扱い、工期 +2日）、Tip「処理を間違えたら、同じ画面の「確認待ちに戻す」ボタンで元に戻せます。」 |
| `others` | 5. その他の画面の使い方 | カード 4 枚：資料 品質チェック／標準化・仕様への昇格（amber Tip「来年度の資料に影響…迷う場合は「保留」」）／図面直読・Web 一次回答・動画マニュアル生成／モデル接続・利用上限（管理者向け）。各 2〜3 箇条書き。 |
| `glossary` | 6. 用語集 | `dl`（`md:grid-cols-[220px_1fr]`）で 8 語：AI 回答案（弟子の提案）／師匠／弟子／適合（◎ ○ △ ×）／信頼度（%）／仕様書との差分／学習キュー／昇格候補／主系／副系／緊急予備。 |
| `faq` | 7. 困ったときは | `<details>` アコーディオン 6 件（Q チップ navy、A チップ emerald）：回答案が間違っている気がする／間違えて承認した／信頼度がオレンジ／手書き図面の注意／数字が初期状態に戻った／AI が止まった。末尾に連絡先案内「解決しないときは、積算担当 A またはシステム管理者に連絡してください。案件番号（例：INQ-2026-0912-03）を伝えるとスムーズです。」 |

- 補助コンポーネント（ページ内定義）：`Section(id,title)`, `Step(no,title)`（navy-700 の番号丸）, `Tip(tone: navy|amber)`（`HelpCircle`／`AlertTriangle` アイコン付き枠）。

### 7.11 新規案件の登録（`/inquiries/new`）
- Client Component。3 ステップのウィザード。state：`step: 1|2|3`、`files: UploadedFile[]`、`form: NewInquiryInput`、`genIndex`（-1 未開始 / 0〜3 進行中 / 4 完了）、`created: Inquiry | null`。
- 上部に「← 一覧へ戻る」。**PageHeader**：eyebrow「AIの弟子入り」、title「新規案件の登録」、phase「第1フェーズ」、description「届いた資料をアップロードすると、AI が条件を読み取り、登録済みの積算資料・仕様書・過去の判断履歴と照らし合わせて回答案を作成します。回答案は「確認待ち」として一覧に追加されます。」
- **ステッパー**：「1 資料をアップロード」「2 読み取った条件を確認」「3 AI 回答案を生成」。完了ステップは emerald 丸＋チェック、現在は navy-700、未到達は slate-200。
- **レイアウト** `lg:grid-cols-3`：左 2 列にステップ本文、右列に固定カード 2 枚。

**STEP 1**
- `Card`「① 案件の資料をアップロード」：破線ドロップ領域（`FileUp`、「図面・柱状図・条件表をここにドロップ」「PDF / JPG / PNG / DXF / Excel（1ファイル 50MB まで）」「モックのためサンプルを追加できます：」）＋ secondary ボタン「＋ 設計図面」「＋ 柱状図（地質調査）」「＋ 条件表（Excel）」（同種は 1 件まで、追加済みは disabled）。
- サンプルファイル（種別／ファイル名／サイズ）：設計図面＝`下水道本管新設_平面縦断図_No5-No6.pdf` 4.2 MB／柱状図（地質調査）＝`地質調査報告書_B-3柱状図.pdf` 1.8 MB／条件表（Excel）＝`施工条件一覧_No5-No6.xlsx` 86 KB。
- 追加すると `readStatus: "読み取り中"`（`Loader2`＋「AI が内容を読み取っています…」）→ **1.4 秒後** に完了。設計図面は「要確認」（amber、note「管底高（発進）の数値がかすれており信頼度 71%。次のステップで確認してください」）、柱状図は「読み取り完了」（note「GL-4.0m 以深に礫混じり砂（最大礫径 35mm）を検出」）、条件表は「読み取り完了」。各行に `X` 削除ボタン。
- 読み取り完了時にフォームへ反映：設計図面 → `pipeDiameter: 250, length: 58, shaft: "発進立坑 φ2.5m ／ 到達立坑 φ2.0m", source: "PDF図面"`；柱状図 → `soil: "砂質土 → 礫混じり砂（GL-4.0m 以深、最大礫径 35mm）", nValue: "N=12〜25", groundwater: "GL-2.3m"`；条件表 → `client` が空なら `"◎◎市 下水道課"`。
- `Card`「② 依頼元・電話メモ（任意）」：依頼元（text）／区分（select：自治体／設計事務所／協会加盟工事会社）／工種（select：本管新設（推進）／取付管（推進）／事前相談）／電話メモ（textarea 2 行、placeholder「例：φ250 で 40m ほど。土質は不明、ボーリング未実施。来月着手したい。」）。
- 右下ボタン「次へ：読み取った条件を確認 →」：ファイルが 1 件以上かつ読み取り中でない、または電話メモが入力済みのとき有効。

**STEP 2**
- `Card`「読み取った条件（編集できます）」：説明「資料から AI が読み取った値です。オレンジの項目は信頼度が低いため、元の資料と見比べて修正してください。」。2 列フォーム：依頼元／入力形式（select：PDF図面／手書き図面／Excel／電話）／管径（mm, number）／推進延長（m, number）／土質（全幅）／N値／地下水位／立坑（全幅。設計図面をアップロード済みなら amber 枠＋注記「管底高（発進）の読み取り信頼度 71%。図面を目視で確認してください」）。
- ボタン「← 戻る」（ghost）／「✨ AI 回答案を生成」（primary。依頼元・管径>0・延長>0・土質 が揃うと有効）。

**STEP 3**
- `Card`「✨ AI 回答案を生成しています」：4 工程を順に進行（各 0.9 秒）：①アップロード資料の読み取り（図面・柱状図・条件表）②積算資料 2026年版・参考資料（仕様書）との照合 ③過去の判断履歴 412件から類似案件を検索（完了時に `Chip navy`「類似 11件」）④機種提案・概算・根拠・注意点の生成。完了＝`CheckCircle2` emerald、進行中＝`Loader2`、未着手＝灰色の丸枠。
- 4 工程完了の 0.3 秒後に `addInquiry(form)` を呼び、emerald 枠「回答案を作成し、「確認待ち」として一覧に追加しました」＋ `FitBadge lg`・id・title・「{machine} ・ 信頼度 {n}% ・ 概算 {yen}」＋ボタン「回答案を確認する（詳細へ）」（`router.push(/inquiries/{id})`）／「一覧へ」。
- サイドバーの確認待ちバッジがこの時点で +1 される。

**右列（全ステップ共通）**
- `Card`「🗄 AI が照らし合わせる参照データ」（action リンク「管理画面へ」→ `/sources`）：`knowledgeSources` のうち `status === "取込済み"` を `BookOpen` 付きで列挙（name／「{category} ・ {volume}（ ・ {version}）」）。注記「回答案の「判断の根拠」「参照した資料」は、ここに登録されたデータから引用されます。古い仕様のデータは除外されています。」
- `Card`「必要な資料」：`Chip rose`「必須」設計図面（平面図・縦断図）… 管径・延長・立坑／`Chip amber`「推奨」柱状図（地質調査）… 土質・N値・地下水位・礫径／`Chip slate`「任意」条件表（Excel）… 依頼元・工期希望など／`Chip slate`「任意」電話メモ … 資料が届く前の事前相談。注記「柱状図がない場合は標準土質を仮定した「暫定判定」になります（信頼度は下がります）。」

### 7.12 参照データ管理（`/sources`）
- Client Component。state：`sources`（seed = `knowledgeSources`）、`filter: SourceCategory | "all"`、`reindexing`。
- **PageHeader**：eyebrow「データの持たせ方が最大の肝」、title「参照データ管理」、phase「第1フェーズ」、description「AI が回答案を作るときに照らし合わせる「積算資料・仕様書・過去の判断履歴」を登録・管理します。仕様変更前の古いデータは混ぜると誤った出力になるため、有効期間で区切って除外できます。ナレッジは特定の AI ツールに閉じず、自社の資産として保持されます。」、actions＝「AI に再取込」（secondary, `RefreshCw`、1.8 秒間「再取込中…」、完了時に `取込中` → `取込済み`）／「ファイルを追加」（primary, `FileUp`、ダミー）。
- **集計 4 カード**：「AI が参照中」= 取込済み件数／「要確認」= 要確認件数（>0 なら rose）／「判断履歴（学習対象）」412件（注記「2023-04-01 以降（仕様変更後）」）／「最終取込」09-12（注記「2026-09-12 08:00 自動」）。
- **カテゴリフィルタ**：すべて＋6 カテゴリ（積算資料／参考資料（仕様書）／判断履歴／施工実績／機種カタログ／土質・地盤データ、件数併記）。カテゴリ選択時はその説明文（`sourceCategoryDesc`）を `Database` アイコン付きで表示。
- **ファイルカード**：`Chip navy`(category)＋`Chip`(status：取込済み=green／取込中=amber＋`Loader2`／要確認=rose／除外=slate)＋「{fileType} ・ {size} ・ {volume}」、name（太字）、「版：{version} ・ 有効期間：{validFrom ?? "—"} 〜 {validTo ?? "現在"} ・ 更新 {updatedAt}」、「用途：{usedFor}」、note があれば枠（要確認は rose＋`AlertTriangle`、それ以外 slate＋`ShieldCheck`）。右ボタン：要確認のとき「確認済みにする」（primary → 取込済み）／「内容を見る」（secondary、ダミー）／「参照対象から除外」（ghost）または「参照対象に戻す」（secondary）。除外カードは `opacity-60`。
- **下段** `Card`「データ登録のルール（現地訪問時に確認する事項）」：2 列 4 項目（1. 有効期間で区切る／2. 判断履歴は「理由」つきで／3. 矛盾は品質チェックへ／4. AI ツールに閉じない）。本文は実装ファイルどおり。

---

## 8. 非機能要件

| 区分 | 要件 |
| --- | --- |
| 表示 | 1440×1000 で横スクロールなし。テーブルは `overflow-x-auto` のコンテナ内でのみ横スクロール。 |
| 性能 | 静的ページは SSG。初期表示にネットワーク依存なし（フォントは next/font でセルフホスト）。 |
| 品質 | `tsc --noEmit`・`eslint`・`next build` がすべて成功。ブラウザコンソールにエラーなし。 |
| 文言 | 「置き換え」ではなく「弟子を育てる」文脈（師匠＝担当者、弟子＝AI）を全画面で一貫させる。 |
| 免責 | サイドバー下部に「モックアプリ：機種・数値・自治体名はすべて架空」を常時表示。 |
| 日付表記 | `YYYY-MM-DD`（ダッシュボード eyebrow のみ「2026-09-12（土）」）。 |

---

## 9. 受け入れ基準（動作確認シナリオ）

1. `/` を開くと KPI 4 枚・確認待ち 3 件・フェーズ 4 枚が表示され、サイドバー「積算判定アシスト」にバッジ「3」が出る。
2. `/inquiries/INQ-2026-0912-02` を開き「修正して回答」→ 本文と判断ルールを入力 →「確定」すると、状態が「修正して回答」に変わり、学習キュー枠に入力したルールが表示され、サイドバーのバッジが「2」に減る。
3. 同画面で「確認待ちに戻す」を押すと状態が「確認待ち」に戻り、バッジが「3」に戻る。
4. `/inquiries` でフィルタ「相談中」を選ぶと `INQ-2026-0911-08` のみ表示される。検索「礫」で該当 2 件（0912-02, 0911-08）に絞られる。
5. `/quality` で未解決 4 件・解決済み 2 件。トグルで移動し集計カードの件数が連動する。「再チェック」押下で 1.5 秒間ボタンが無効化される。
6. `/knowledge` で KC-001 の「採用する」を押すとステータスが「採用予定」になり、ヘッダーボタンの件数が 2 → 3 に増える。
7. `/drawings` で「サンプル図面で試す」→ 約 1.8 秒後に読み取り結果 8 行と機種提案カードが表示される。
8. `/web-answer` で「礫混じり」を含む質問を送信すると礫径別の回答が返る。それ以外は default 回答。
9. `/manuals` でジョブを切り替えると右カードが 生成済み／生成中／未着手 の 3 パターンで切り替わる。
10. `/settings` で「GPT」の「主系に切替」を押すと GPT が主系、Claude が副系表示になる。
11. `/guide` の目次「4. 積算判定アシストの使い方」をクリックすると `main` 内が該当セクションまでスクロールし、サイドバーと目次は固定されたまま表示される。ダッシュボード右上「はじめての方は操作ガイドへ」から `/guide` に遷移できる。
12. `/inquiries/new` で「＋ 設計図面」「＋ 柱状図（地質調査）」「＋ 条件表（Excel）」を追加 → 約 1.4 秒後に読み取り完了し、依頼元「◎◎市 下水道課」・管径 250・延長 58・土質「砂質土 → 礫混じり砂（…最大礫径 35mm）」が自動入力される。「次へ」→「AI 回答案を生成」で 4 工程が順に完了し、`INQ-2026-MMDD-NN` の案件が **BM-R型・適合 △・確認待ち** として作成され、サイドバーのバッジが +1 される。「回答案を確認する（詳細へ）」で詳細画面に遷移できる。
13. `/sources` で KS-006（要確認）の「確認済みにする」を押すと取込済みになり、「要確認」カードが 1 → 0 になる。KS-004（除外）の「参照対象に戻す」で取込済みに変わる。「AI に再取込」で KS-007 が取込中 → 取込済みになる。

---

## 付録 A：`src/lib/types.ts`（全文）

```ts
export type InquiryStatus =
  | "pending_review" // AI回答案あり・担当者確認待ち
  | "approved" // 担当者承認済み
  | "revised" // 担当者が修正して回答
  | "escalated" // 現場経験者・協会内へ相談中
  | "draft"; // AI回答案 生成中

export type Fit = "◎" | "○" | "△" | "×";

export type SourceType = "PDF図面" | "手書き図面" | "Excel" | "電話";

export interface Reference {
  doc: string;
  page: string;
  label: string;
}

export interface AiProposal {
  machine: string;
  fit: Fit;
  confidence: number; // 0-100
  estimate: number; // 円
  dailyAdvance: number; // m/日
  durationDays: number;
  rationale: string[];
  cautions: string[];
  references: Reference[];
  deviationFromSpec?: string; // 仕様書との差分（経験値）
}

export interface Inquiry {
  id: string;
  receivedAt: string; // ISO
  client: string;
  clientType: "自治体" | "設計事務所" | "協会加盟工事会社";
  title: string;
  workType: string;
  pipeDiameter: number; // mm
  length: number; // m
  soil: string;
  nValue: string;
  groundwater: string;
  shaft: string;
  source: SourceType;
  status: InquiryStatus;
  proposal: AiProposal;
  reviewerNote?: string;
  reviewer?: string;
  learnedDelta?: string; // 学習キューに入った差分
}

export interface KnowledgeCandidate {
  id: string;
  pattern: string;
  currentSpec: string;
  observedPractice: string;
  occurrences: number;
  period: string;
  suggestion: string;
  targetDoc: string;
  status: "提案中" | "採用予定" | "保留" | "却下";
  evidence: string[];
}

export type IssueType = "抜け漏れ" | "外れ値" | "不整合" | "改定漏れ";
export type Severity = "高" | "中" | "低";

export interface QualityIssue {
  id: string;
  type: IssueType;
  severity: Severity;
  doc: string;
  location: string;
  description: string;
  suggestion: string;
  resolved: boolean;
}

export interface ModelProvider {
  id: string;
  name: string;
  vendor: string;
  role: "主系" | "副系" | "緊急予備（ローカル）";
  status: "稼働中" | "待機" | "障害";
  latencyMs: number;
  monthlyCost: number;
  note: string;
}

export interface ManualJob {
  id: string;
  title: string;
  videoLength: string;
  uploadedAt: string;
  progress: number; // 0-100
  status: "生成済み" | "生成中" | "未着手";
  steps?: { no: number; title: string; body: string; timestamp: string }[];
}

// ---- 参照データ（AI が回答案を作るときに照らし合わせるナレッジ） ----
export type SourceCategory =
  | "積算資料"
  | "参考資料（仕様書）"
  | "判断履歴"
  | "施工実績"
  | "機種カタログ"
  | "土質・地盤データ";

export type SourceStatus = "取込済み" | "取込中" | "要確認" | "除外";

export interface KnowledgeSource {
  id: string;
  category: SourceCategory;
  name: string;
  fileType: "PDF" | "Excel" | "CSV" | "CAD" | "画像";
  size: string;
  version?: string;
  validFrom?: string; // YYYY-MM-DD
  validTo?: string; // YYYY-MM-DD
  volume: string; // "48ページ" / "412件"
  status: SourceStatus;
  updatedAt: string; // YYYY-MM-DD
  usedFor: string;
  note?: string;
}

// ---- 新規案件の入力資料 ----
export type UploadKind = "設計図面" | "柱状図（地質調査）" | "条件表（Excel）" | "電話メモ";

export interface UploadedFile {
  id: string;
  kind: UploadKind;
  name: string;
  size: string;
  readStatus: "読み取り中" | "読み取り完了" | "要確認";
  note?: string;
}

export interface NewInquiryInput {
  client: string;
  clientType: Inquiry["clientType"];
  workType: string;
  pipeDiameter: number;
  length: number;
  soil: string;
  nValue: string;
  groundwater: string;
  shaft: string;
  source: SourceType;
  memo?: string;
}
```

## 付録 B：`src/lib/mock-data.ts`（全文）

```ts
import type {
  Inquiry,
  KnowledgeCandidate,
  KnowledgeSource,
  ManualJob,
  ModelProvider,
  QualityIssue,
} from "./types";

// ※ 本データはモックです。機種名・数値・自治体名はすべて架空のものです。

export const MACHINES = [
  { code: "BM-S", name: "BM-S型 小口径削進機", range: "φ100〜150" },
  { code: "BM-M", name: "BM-M型 小口径削進機", range: "φ200〜250" },
  { code: "BM-L", name: "BM-L型 中口径削進機", range: "φ300〜400" },
  { code: "BM-R", name: "BM-R型 礫対応削進機", range: "φ200〜300（礫層）" },
];

export const inquiries: Inquiry[] = [
  {
    id: "INQ-2026-0912-03",
    receivedAt: "2026-09-12T09:42:00+09:00",
    client: "某市 下水道課",
    clientType: "自治体",
    title: "本管新設 φ200 推進延長 48m（砂質土）",
    workType: "本管新設（推進）",
    pipeDiameter: 200,
    length: 48,
    soil: "砂質土（細砂主体）",
    nValue: "N=8〜12",
    groundwater: "GL-2.1m",
    shaft: "発進立坑 φ2.5m ／ 到達立坑 φ2.0m",
    source: "PDF図面",
    status: "pending_review",
    proposal: {
      machine: "BM-M型 小口径削進機",
      fit: "◎",
      confidence: 91,
      estimate: 4_860_000,
      dailyAdvance: 8,
      durationDays: 6,
      rationale: [
        "管径 φ200・延長 48m は BM-M型 の標準適用範囲（φ200〜250、〜60m）に収まる",
        "砂質土 N=8〜12 は積算資料 2026年版 表3-2 の『砂質土 区分B』に該当",
        "地下水位 GL-2.1m は管底より上のため、補助工法（薬液注入）の要否を条件に加算",
      ],
      cautions: [
        "地下水位が管底より高いため、補助工法の要否を設計者へ確認すること",
        "到達立坑 φ2.0m は最小寸法。到達側の機材搬出手順に注意",
      ],
      references: [
        { doc: "積算資料 2026年版", page: "p.12 表3-2", label: "土質区分と日進量" },
        { doc: "積算資料 2026年版", page: "p.18 表5-1", label: "機種別 標準歩掛" },
        { doc: "参考資料（仕様書）", page: "p.7", label: "BM-M型 適用範囲" },
      ],
    },
  },
  {
    id: "INQ-2026-0912-02",
    receivedAt: "2026-09-12T09:05:00+09:00",
    client: "△△設計事務所",
    clientType: "設計事務所",
    title: "取付管 φ100 指定 延長 22m（礫混じり砂）",
    workType: "取付管（推進）",
    pipeDiameter: 100,
    length: 22,
    soil: "礫混じり砂（最大礫径 40mm）",
    nValue: "N=15〜20",
    groundwater: "GL-3.5m",
    shaft: "発進立坑 φ2.0m ／ 到達 既設人孔",
    source: "手書き図面",
    status: "pending_review",
    proposal: {
      machine: "BM-R型 礫対応削進機（φ200 へ変更提案）",
      fit: "△",
      confidence: 74,
      estimate: 3_120_000,
      dailyAdvance: 5,
      durationDays: 5,
      rationale: [
        "客先指定は φ100 だが、最大礫径 40mm の礫混じり砂では BM-S型（φ100）の排土能力を超える",
        "過去の類似案件 14件中 12件で φ200・BM-R型 へ変更して施工している（判断履歴 2023〜2026）",
        "仕様書上は φ100 でも『適用可』の記載だが、経験値では通らない可能性が高い",
      ],
      cautions: [
        "客先指定径からの変更提案となるため、設計者への説明資料が必要",
        "手書き図面のため、管底高・立坑位置の読み取り精度に注意（要目視確認）",
      ],
      references: [
        { doc: "参考資料（仕様書）", page: "p.5", label: "BM-S型 適用範囲（礫径 ≤ 20mm）" },
        { doc: "判断履歴", page: "2023-2026", label: "礫混じり砂 φ100指定 案件 14件" },
      ],
      deviationFromSpec:
        "仕様書では φ100（BM-S型）適用可だが、経験則上 礫径 40mm では φ200（BM-R型）を推奨。",
    },
  },
  {
    id: "INQ-2026-0912-01",
    receivedAt: "2026-09-12T08:30:00+09:00",
    client: "○○建設（協会加盟）",
    clientType: "協会加盟工事会社",
    title: "本管新設 φ300 推進延長 75m（粘性土）",
    workType: "本管新設（推進）",
    pipeDiameter: 300,
    length: 75,
    soil: "粘性土（シルト質）",
    nValue: "N=3〜6",
    groundwater: "GL-1.8m",
    shaft: "発進立坑 φ3.0m ／ 到達立坑 φ2.5m",
    source: "Excel",
    status: "pending_review",
    proposal: {
      machine: "BM-L型 中口径削進機",
      fit: "○",
      confidence: 83,
      estimate: 9_450_000,
      dailyAdvance: 6,
      durationDays: 13,
      rationale: [
        "φ300・75m は BM-L型 の適用範囲内だが、延長が標準（60m）を超えるため中間ジャッキの検討が必要",
        "軟弱粘性土（N=3〜6）は蛇行しやすく、日進量を標準の 8m/日 から 6m/日 に低減",
      ],
      cautions: [
        "延長 60m 超のため、推力計算と中間ジャッキ要否を積算担当が確認",
        "N値 5 以下の区間は方向制御の頻度が上がる。過去案件では工期 +2日 の実績あり",
      ],
      references: [
        { doc: "積算資料 2026年版", page: "p.14 表3-4", label: "軟弱土 日進量補正" },
        { doc: "参考資料（仕様書）", page: "p.9", label: "BM-L型 適用範囲・最大推進延長" },
      ],
    },
  },
  {
    id: "INQ-2026-0911-08",
    receivedAt: "2026-09-11T16:20:00+09:00",
    client: "□□町 上下水道課",
    clientType: "自治体",
    title: "本管新設 φ250 推進延長 35m（砂礫）",
    workType: "本管新設（推進）",
    pipeDiameter: 250,
    length: 35,
    soil: "砂礫（最大礫径 80mm）",
    nValue: "N=30以上",
    groundwater: "GL-4.0m",
    shaft: "発進立坑 φ2.5m ／ 到達立坑 φ2.5m",
    source: "PDF図面",
    status: "escalated",
    proposal: {
      machine: "BM-R型 礫対応削進機",
      fit: "△",
      confidence: 58,
      estimate: 5_900_000,
      dailyAdvance: 3,
      durationDays: 12,
      rationale: [
        "最大礫径 80mm は BM-R型 の上限（60mm）を超える。過去に同条件での施工実績が 2件しかない",
      ],
      cautions: ["実績が少なく信頼度が低い。現場経験者への相談を推奨"],
      references: [
        { doc: "参考資料（仕様書）", page: "p.11", label: "BM-R型 礫径上限" },
      ],
    },
    reviewer: "積算担当 A",
    reviewerNote: "礫径 80mm は判断が割れる。現場経験者（協会 技術委員）へ相談中。",
  },
  {
    id: "INQ-2026-0911-05",
    receivedAt: "2026-09-11T13:10:00+09:00",
    client: "◇◇市 下水道整備課",
    clientType: "自治体",
    title: "取付管 φ150 延長 18m（砂質土）",
    workType: "取付管（推進）",
    pipeDiameter: 150,
    length: 18,
    soil: "砂質土",
    nValue: "N=10〜15",
    groundwater: "GL-3.0m",
    shaft: "発進立坑 φ2.0m ／ 到達 既設人孔",
    source: "PDF図面",
    status: "approved",
    proposal: {
      machine: "BM-S型 小口径削進機",
      fit: "◎",
      confidence: 95,
      estimate: 1_980_000,
      dailyAdvance: 8,
      durationDays: 3,
      rationale: ["φ150・18m・砂質土は BM-S型 の最も標準的な適用条件"],
      cautions: [],
      references: [
        { doc: "積算資料 2026年版", page: "p.12 表3-2", label: "土質区分と日進量" },
      ],
    },
    reviewer: "積算担当 A",
    reviewerNote: "AI回答案どおりでOK。",
  },
  {
    id: "INQ-2026-0911-02",
    receivedAt: "2026-09-11T10:45:00+09:00",
    client: "▽▽設計コンサルタント",
    clientType: "設計事務所",
    title: "本管新設 φ200 推進延長 55m（粘性土・地下水高）",
    workType: "本管新設（推進）",
    pipeDiameter: 200,
    length: 55,
    soil: "粘性土",
    nValue: "N=4〜8",
    groundwater: "GL-1.2m",
    shaft: "発進立坑 φ2.5m ／ 到達立坑 φ2.0m",
    source: "PDF図面",
    status: "revised",
    proposal: {
      machine: "BM-M型 小口径削進機",
      fit: "○",
      confidence: 80,
      estimate: 5_200_000,
      dailyAdvance: 7,
      durationDays: 8,
      rationale: ["φ200・55m は BM-M型 適用範囲内"],
      cautions: ["地下水位が高いため補助工法を検討"],
      references: [
        { doc: "積算資料 2026年版", page: "p.12 表3-2", label: "土質区分と日進量" },
      ],
    },
    reviewer: "積算担当 A",
    reviewerNote:
      "地下水位 GL-1.2m で粘性土の場合、発進部の鏡切り時に湧水リスクが高い。薬液注入を『検討』ではなく『必須』として回答。工期 +2日。",
    learnedDelta:
      "条件: 粘性土 × 地下水位が管頂より 1.0m 以上高い → 補助工法（薬液注入）を必須扱い、工期 +2日",
  },
  {
    id: "INQ-2026-0910-04",
    receivedAt: "2026-09-10T15:00:00+09:00",
    client: "☆☆工業（協会加盟）",
    clientType: "協会加盟工事会社",
    title: "更生前 事前相談 φ250 延長 40m",
    workType: "事前相談",
    pipeDiameter: 250,
    length: 40,
    soil: "不明（ボーリング未実施）",
    nValue: "—",
    groundwater: "—",
    shaft: "未定",
    source: "電話",
    status: "approved",
    proposal: {
      machine: "BM-M型 小口径削進機（暫定）",
      fit: "○",
      confidence: 62,
      estimate: 4_400_000,
      dailyAdvance: 7,
      durationDays: 6,
      rationale: ["土質データ未取得のため、標準土質（砂質土 区分B）を仮定した暫定判定"],
      cautions: ["土質調査結果の提出を依頼し、確定判定は再度実施"],
      references: [
        { doc: "積算資料 2026年版", page: "p.12 表3-2", label: "土質区分と日進量" },
      ],
    },
    reviewer: "積算担当 A",
    reviewerNote: "暫定回答として送付。ボーリングデータ待ち。",
  },
];

export const knowledgeCandidates: KnowledgeCandidate[] = [
  {
    id: "KC-001",
    pattern: "礫混じり砂（礫径 20〜40mm）× φ100 指定",
    currentSpec: "参考資料 p.5：BM-S型（φ100）適用可（礫径 ≤ 20mm）",
    observedPractice: "φ200（BM-R型）へ変更して回答",
    occurrences: 14,
    period: "2023-04 〜 2026-08",
    suggestion:
      "参考資料 p.5 の BM-S型 適用条件に『礫径 20mm 超の場合は BM-R型（φ200）を選定』の注記を追加し、積算資料 表3-2 に礫径 20〜40mm の区分を新設する。",
    targetDoc: "2027年版 積算資料 / 参考資料（仕様書）",
    status: "提案中",
    evidence: [
      "INQ-2025-0314-02 ほか 13件で同一判断",
      "変更せず φ100 で施工した 2件はいずれも途中で機種変更（工期 +5日）",
    ],
  },
  {
    id: "KC-002",
    pattern: "粘性土 × 地下水位が管頂より 1.0m 以上高い",
    currentSpec: "積算資料 p.20：補助工法は『必要に応じて検討』",
    observedPractice: "薬液注入を必須として積算、工期 +2日",
    occurrences: 9,
    period: "2024-01 〜 2026-09",
    suggestion:
      "『検討』を『必須』に格上げし、標準歩掛に薬液注入 1式 と 工期補正 +2日 を明記する。",
    targetDoc: "2027年版 積算資料",
    status: "採用予定",
    evidence: ["INQ-2026-0911-02 で担当者が修正（学習キュー登録済み）", "過去 9件すべて同一判断"],
  },
  {
    id: "KC-003",
    pattern: "推進延長 60〜80m × BM-L型",
    currentSpec: "参考資料 p.9：最大推進延長 60m",
    observedPractice: "中間ジャッキ併用で 80m まで施工実績あり（6件）",
    occurrences: 6,
    period: "2022-06 〜 2026-05",
    suggestion:
      "最大推進延長を『60m（中間ジャッキ併用時 80m）』へ改定し、中間ジャッキの歩掛を追加する。",
    targetDoc: "2027年版 参考資料（仕様書）",
    status: "保留",
    evidence: ["6件中 1件で推力超過によりやり直し。条件付き採用を検討"],
  },
  {
    id: "KC-004",
    pattern: "到達側 既設人孔 × φ150 以上",
    currentSpec: "記載なし",
    observedPractice: "人孔内壁のコア抜き径を φ+100mm で指定",
    occurrences: 21,
    period: "2021-10 〜 2026-08",
    suggestion: "参考資料に『到達人孔 コア抜き径 = 管径 + 100mm』を標準として明記する。",
    targetDoc: "2027年版 参考資料（仕様書）",
    status: "採用予定",
    evidence: ["電話問い合わせの約 15% がこの質問。明記により問い合わせ削減が見込める"],
  },
];

export const qualityIssues: QualityIssue[] = [
  {
    id: "QI-001",
    type: "抜け漏れ",
    severity: "高",
    doc: "積算資料 2026年版",
    location: "p.18 表5-1 機種別標準歩掛",
    description: "BM-R型 の φ250 行で『日進量（m/日）』が空欄。φ200・φ300 は記載あり。",
    suggestion: "φ200（5m/日）と φ300（4m/日）の間で 4.5m/日 が妥当。過去実績 8件の平均は 4.6m/日。",
    resolved: false,
  },
  {
    id: "QI-002",
    type: "外れ値",
    severity: "高",
    doc: "積算資料 2026年版",
    location: "p.22 表6-3 労務単価",
    description: "『礫層 世話役』の単価が他の土質区分の 3.2倍。桁の入力ミスの可能性。",
    suggestion: "他区分は 28,000〜31,000円。当該セルは 96,000円 → 29,600円 の誤記と推定。",
    resolved: false,
  },
  {
    id: "QI-003",
    type: "不整合",
    severity: "中",
    doc: "積算資料 2026年版 / 参考資料（仕様書）",
    location: "積算 p.9 表2-1 ／ 仕様書 p.7",
    description: "BM-M型 の機械質量が積算資料では 1,850kg、仕様書では 1,920kg と異なる。",
    suggestion: "2025年のマイナーチェンジ後の質量（1,920kg）に統一。積算側の更新漏れと推定。",
    resolved: false,
  },
  {
    id: "QI-004",
    type: "改定漏れ",
    severity: "中",
    doc: "参考資料（仕様書）",
    location: "p.15 補助工法",
    description: "2025年版で廃止された『高圧噴射攪拌 A工法』の記載が残存。",
    suggestion: "該当段落を削除し、現行の B工法 の記載へ差し替え。",
    resolved: false,
  },
  {
    id: "QI-005",
    type: "抜け漏れ",
    severity: "低",
    doc: "積算資料 2026年版",
    location: "p.31 付表",
    description: "到達立坑 φ2.0m の場合の機材搬出手順への参照番号が欠落。",
    suggestion: "参考資料 p.12 への参照を追記。",
    resolved: true,
  },
  {
    id: "QI-006",
    type: "不整合",
    severity: "低",
    doc: "積算資料 2026年版",
    location: "p.12 表3-2 ／ p.14 表3-4",
    description: "『砂質土 区分B』の N値範囲が 表3-2 では 8〜15、表3-4 では 8〜12 と異なる。",
    suggestion: "表3-4 に合わせて 8〜12 へ統一（区分C との重複を解消）。",
    resolved: true,
  },
];

export const modelProviders: ModelProvider[] = [
  {
    id: "claude",
    name: "Claude（フロンティアモデル）",
    vendor: "Anthropic API",
    role: "主系",
    status: "稼働中",
    latencyMs: 2100,
    monthlyCost: 12_400,
    note: "積算判定・根拠生成・図面読取（マルチモーダル）",
  },
  {
    id: "gpt",
    name: "GPT（フロンティアモデル）",
    vendor: "OpenAI API",
    role: "副系",
    status: "待機",
    latencyMs: 2400,
    monthlyCost: 5_840,
    note: "主系の障害・提供終了時に自動切替。品質チェックの二重確認にも使用",
  },
  {
    id: "local",
    name: "ローカル小型モデル",
    vendor: "社内サーバー（オンプレ）",
    role: "緊急予備（ローカル）",
    status: "待機",
    latencyMs: 6800,
    monthlyCost: 0,
    note: "外部 API 断時に『ぎりぎり受け答えできる予備』として動作。精度は限定的",
  },
];

export const manualJobs: ManualJob[] = [
  {
    id: "MJ-001",
    title: "BM-M型 立坑内据付・芯出し作業",
    videoLength: "12:34",
    uploadedAt: "2026-09-10",
    progress: 100,
    status: "生成済み",
    steps: [
      { no: 1, title: "据付架台の搬入", body: "クレーンで据付架台を立坑底へ降ろし、発進方向に対して基準墨に合わせて仮置きする。", timestamp: "00:42" },
      { no: 2, title: "レベル調整", body: "架台四隅のジャッキボルトで水平を出す。水準器で前後左右 ±1mm 以内に調整。", timestamp: "03:15" },
      { no: 3, title: "本体の据付", body: "削進機本体を架台に載せ、固定ボルト 8本 を対角順に本締めする（トルク 120N·m）。", timestamp: "05:50" },
      { no: 4, title: "芯出し", body: "トランシットで発進方向の芯を確認。レーザーターゲットの中心が計画線上にあることを確認する。", timestamp: "08:20" },
      { no: 5, title: "反力受けの設置", body: "後方反力受けを立坑壁に密着させ、隙間はライナープレートで調整する。", timestamp: "10:48" },
    ],
  },
  {
    id: "MJ-002",
    title: "推進管の接続・シール確認",
    videoLength: "08:11",
    uploadedAt: "2026-09-12",
    progress: 62,
    status: "生成中",
  },
  {
    id: "MJ-003",
    title: "到達後の機体撤去・洗浄",
    videoLength: "15:02",
    uploadedAt: "2026-09-12",
    progress: 0,
    status: "未着手",
  },
];

export const dashboardStats = {
  todayInquiries: 10,
  pendingReview: 3,
  approvedWithoutEdit: 78, // %
  avgFirstAnswerMin: 4,
  legacyAvgFirstAnswerMin: 35,
  learningQueue: 5,
  monthlyBudget: 50_000,
  monthlyUsed: 18_240,
};

export const phases = [
  {
    no: "第0",
    name: "動画→作業マニュアル自動生成",
    scope: "現場の作業動画から手順書を自動生成。作業員の引継ぎ・新人教育に活用。",
    period: "1〜1.5ヶ月",
    budget: "30〜50万円",
    status: "稼働中" as const,
    href: "/manuals",
  },
  {
    no: "第1",
    name: "AIの弟子入り（社内利用）",
    scope: "積算判定アシスト＋資料の抜け漏れ・外れ値チェック。AIが回答案を出し、担当者が確認。",
    period: "2〜3ヶ月",
    budget: "100万円前後",
    status: "稼働中" as const,
    href: "/inquiries",
  },
  {
    no: "第2",
    name: "図面直読＋標準仕様への昇格提案",
    scope: "設計図面をそのまま読み取り機種提案。例外対応を束ねて標準仕様へ昇格させる提案。",
    period: "要件次第",
    budget: "追加見積",
    status: "プレビュー" as const,
    href: "/drawings",
  },
  {
    no: "第3",
    name: "Web一次回答／AI向けデータセット整備",
    scope: "着手前の相談にWeb上でAIが一次回答。他社CAD搭載AIが読むための積算データを整備。",
    period: "要件次第",
    budget: "追加見積",
    status: "構想" as const,
    href: "/web-answer",
  },
];

// ---- 参照データ（AI が照らし合わせるナレッジ）----
export const knowledgeSources: KnowledgeSource[] = [
  {
    id: "KS-001",
    category: "積算資料",
    name: "積算資料_2026年版.pdf",
    fileType: "PDF",
    size: "18.4 MB",
    version: "2026年版",
    validFrom: "2026-04-01",
    volume: "48ページ",
    status: "取込済み",
    updatedAt: "2026-09-01",
    usedFor: "土質区分・日進量・標準歩掛・労務単価の照合（概算費用の算出根拠）",
  },
  {
    id: "KS-002",
    category: "参考資料（仕様書）",
    name: "参考資料_仕様書_2026年版.pdf",
    fileType: "PDF",
    size: "9.7 MB",
    version: "2026年版",
    validFrom: "2026-04-01",
    volume: "32ページ",
    status: "取込済み",
    updatedAt: "2026-09-01",
    usedFor: "各機種の適用範囲（管径・延長・礫径・土質）の判定",
  },
  {
    id: "KS-003",
    category: "判断履歴",
    name: "積算判定履歴_2023-2026.xlsx",
    fileType: "Excel",
    size: "2.1 MB",
    validFrom: "2023-04-01",
    volume: "412件",
    status: "取込済み",
    updatedAt: "2026-09-10",
    usedFor: "類似案件の検索、仕様書との差分（経験則）の抽出、標準化候補の集計",
    note: "2023-04 の仕様変更以降のみ取り込み。担当者コメント列を「判断理由」として学習",
  },
  {
    id: "KS-004",
    category: "判断履歴",
    name: "積算判定履歴_2019-2023.xlsx",
    fileType: "Excel",
    size: "3.4 MB",
    validFrom: "2019-04-01",
    validTo: "2023-03-31",
    volume: "568件",
    status: "除外",
    updatedAt: "2026-09-10",
    usedFor: "—",
    note: "旧仕様（BM-M型 改良前・旧歩掛）のデータ。混ぜると誤った出力になるため参照対象から除外",
  },
  {
    id: "KS-005",
    category: "施工実績",
    name: "協会_施工実績台帳_2024-2026.csv",
    fileType: "CSV",
    size: "640 KB",
    validFrom: "2024-01-01",
    volume: "186件",
    status: "取込済み",
    updatedAt: "2026-08-28",
    usedFor: "実績日進量・工期実績による注意点の補強（例：N値 5 以下で工期 +2日）",
  },
  {
    id: "KS-006",
    category: "機種カタログ",
    name: "BMシリーズ_機種カタログ_2025.pdf",
    fileType: "PDF",
    size: "24.0 MB",
    version: "2025年版",
    volume: "20ページ",
    status: "要確認",
    updatedAt: "2026-09-11",
    usedFor: "機械質量・寸法・必要立坑径の照合",
    note: "BM-M型 の機械質量が積算資料と不一致（品質チェック QI-003）。どちらを正とするか確認待ち",
  },
  {
    id: "KS-007",
    category: "土質・地盤データ",
    name: "地盤区分早見表_協会技術委員会.xlsx",
    fileType: "Excel",
    size: "310 KB",
    volume: "1シート",
    status: "取込中",
    updatedAt: "2026-09-12",
    usedFor: "柱状図の土質名・N値から積算資料の土質区分（A〜D）への変換",
  },
];

export const sourceCategoryDesc: Record<KnowledgeSource["category"], string> = {
  積算資料: "概算費用の算出根拠。年度版ごとに管理し、有効期間で切り替える",
  "参考資料（仕様書）": "機種の適用範囲。仕様書との差分（経験則）を検出する基準",
  判断履歴: "過去の担当者の判断。類似案件の検索と経験則の抽出に使う。仕様変更前は除外",
  施工実績: "実際の日進量・工期。注意点の根拠を補強する",
  機種カタログ: "機械の寸法・質量。立坑条件との整合に使う",
  "土質・地盤データ": "柱状図の読み替えルール。図面直読の精度に効く",
};
```

## 付録 C：`src/lib/format.ts`（全文）

```ts
export const yen = (n: number) =>
  new Intl.NumberFormat("ja-JP", { style: "currency", currency: "JPY", maximumFractionDigits: 0 }).format(n);

export const dateTime = (iso: string) =>
  new Intl.DateTimeFormat("ja-JP", {
    month: "2-digit",
    day: "2-digit",
    hour: "2-digit",
    minute: "2-digit",
    timeZone: "Asia/Tokyo",
  }).format(new Date(iso));

export const statusLabel: Record<string, string> = {
  pending_review: "確認待ち",
  approved: "承認済み",
  revised: "修正して回答",
  escalated: "相談中",
  draft: "AI生成中",
};
```

## 付録 D：`src/app/globals.css`（全文）

```css
@import "tailwindcss";

@theme {
  --color-navy-50: #eef2f8;
  --color-navy-100: #d9e1ef;
  --color-navy-200: #b3c3df;
  --color-navy-300: #7f9ac5;
  --color-navy-500: #3b5a8c;
  --color-navy-600: #2c4772;
  --color-navy-700: #1f3559;
  --color-navy-800: #172845;
  --color-navy-900: #0f1b30;
  --font-sans: var(--font-noto-sans-jp), "Hiragino Sans", "Hiragino Kaku Gothic ProN", "Yu Gothic", sans-serif;
}

html {
  color-scheme: light;
}

body {
  background: #f4f6fa;
  color: #0f172a;
  font-family: var(--font-sans);
  font-feature-settings: "palt";
}
```

## 付録 E：Web 一次回答の固定文言

**初期メッセージ**

- user：
  ```
  φ200 の下水道本管を 40m ほど推進したいのですが、砂質土でベビーモール工法は使えますか？
  ```
- ai：
  ```
  はい、φ200・延長 40m・砂質土であれば、BM-M型 小口径削進機の標準適用範囲内です。

  ■ 目安
  ・日進量：約 8m/日
  ・想定工期：5〜6日（据付・撤去含む）

  ■ 確認が必要な点
  ・地下水位が管底より高い場合は補助工法（薬液注入）の検討が必要です
  ・到達側が既設人孔の場合、コア抜き径は管径 +100mm が標準です

  詳細な積算や図面をお持ちの場合は、協会へお送りいただければ担当者が確認のうえ回答します。
  ```

**自動応答（`canned`）**

- `default`：
  ```
  ご質問ありがとうございます。一次回答として、条件（管径・延長・土質・地下水位）をお知らせいただければ、適用可否の目安をお答えできます。

  確定的な判断が必要な場合は、下の「協会へ相談する」からお問い合わせください。担当者が図面を確認して回答します。
  ```
- `礫`（入力に「礫」を含む場合）：
  ```
  礫が混じる地盤では、礫径によって機種が変わります。
  ・礫径 20mm 以下：BM-S型／BM-M型 で対応可
  ・礫径 20〜60mm：BM-R型（礫対応）を推奨
  ・礫径 60mm 超：施工実績が限られるため、協会へ個別にご相談ください

  柱状図があれば、より正確な目安をお伝えできます。
  ```

**データセット JSON 例（表示のみ）**
```json
{
  "machine": "BM-M",
  "pipe_diameter_mm": [200, 250],
  "max_length_m": 60,
  "soil": ["sand", "clay"],
  "advance_m_per_day": 8
}
```

## 付録 F：使用アイコン一覧（lucide-react）

`Activity, AlertTriangle, ArrowLeft, ArrowLeftRight, ArrowRight, Ban, BookOpen, BookOpenCheck, Bot, Check, CheckCircle2, CircleDashed, Clapperboard, ClipboardCheck, Clock, Database, FileDown, FileSpreadsheet, FileText, FileUp, Globe, GraduationCap, HelpCircle, Inbox, KeyRound, LayoutDashboard, Loader2, Image, MessageSquareWarning, PencilLine, Phone, Play, Plus, RefreshCw, RotateCcw, ScanLine, ScanSearch, Search, Send, Settings, ShieldAlert, ShieldCheck, Sparkles, ThumbsUp, Upload, User, Video, Wallet, X`

## 付録 G：`src/lib/mock-ai.ts`（全文）

```ts
import type { AiProposal, Fit, Inquiry, NewInquiryInput } from "./types";

// ※ モック用の簡易ロジック。実際は積算資料・仕様書・判断履歴を参照する AI が生成する。

const MAX_GRAVEL_RE = /礫径\s*(\d+)\s*mm/;

function pickMachine(d: number, soil: string) {
  const gravel = soil.match(MAX_GRAVEL_RE);
  const gravelMm = gravel ? Number(gravel[1]) : soil.includes("礫") ? 30 : 0;
  if (gravelMm > 20) return { machine: "BM-R型 礫対応削進機", code: "BM-R", gravelMm };
  if (d <= 150) return { machine: "BM-S型 小口径削進機", code: "BM-S", gravelMm };
  if (d <= 250) return { machine: "BM-M型 小口径削進機", code: "BM-M", gravelMm };
  return { machine: "BM-L型 中口径削進機", code: "BM-L", gravelMm };
}

const UNIT: Record<string, { perM: number; base: number; advance: number }> = {
  "BM-S": { perM: 78_000, base: 560_000, advance: 8 },
  "BM-M": { perM: 92_000, base: 640_000, advance: 8 },
  "BM-L": { perM: 118_000, base: 900_000, advance: 8 },
  "BM-R": { perM: 110_000, base: 780_000, advance: 5 },
};

export function generateProposal(input: NewInquiryInput): AiProposal {
  const { machine, code, gravelMm } = pickMachine(input.pipeDiameter, input.soil);
  const u = UNIT[code];
  const soft = /粘性|シルト|軟弱/.test(input.soil);
  const highWater = /GL-([0-9.]+)/.test(input.groundwater) && Number(input.groundwater.match(/GL-([0-9.]+)/)![1]) < 2.5;
  const overLength = input.length > 60;
  const handwritten = input.source === "手書き図面";
  const unknownSoil = /不明|未実施|—/.test(input.soil);

  let advance = u.advance;
  if (soft) advance = Math.max(3, advance - 2);
  const durationDays = Math.ceil(input.length / advance) + (soft && highWater ? 2 : 0);

  let fit: Fit = "◎";
  let confidence = 92;
  const rationale: string[] = [];
  const cautions: string[] = [];
  const references = [
    { doc: "積算資料 2026年版", page: "p.12 表3-2", label: "土質区分と日進量" },
    { doc: "積算資料 2026年版", page: "p.18 表5-1", label: "機種別 標準歩掛" },
  ];
  let deviationFromSpec: string | undefined;

  rationale.push(`管径 φ${input.pipeDiameter}・延長 ${input.length}m に対し、${machine} の適用範囲で判定`);

  if (gravelMm > 20) {
    fit = "△";
    confidence -= 16;
    rationale.push(`最大礫径 ${gravelMm}mm は BM-S/BM-M型 の上限（20mm）を超えるため礫対応機を選定`);
    references.push({ doc: "判断履歴", page: "2023-2026", label: "礫混じり地盤 案件 14件" });
    if (input.pipeDiameter <= 150) {
      deviationFromSpec = `仕様書では φ${input.pipeDiameter} 適用可だが、経験則上 礫径 ${gravelMm}mm では φ200（BM-R型）を推奨。`;
      cautions.push("客先指定径からの変更提案となるため、設計者への説明資料が必要");
    }
  }
  if (soft) {
    if (fit === "◎") fit = "○";
    confidence -= 6;
    rationale.push(`軟弱な粘性土のため日進量を標準 ${u.advance}m/日 から ${advance}m/日 に低減`);
    references.push({ doc: "積算資料 2026年版", page: "p.14 表3-4", label: "軟弱土 日進量補正" });
  }
  if (highWater) {
    if (fit === "◎") fit = "○";
    confidence -= 4;
    cautions.push(
      soft
        ? "粘性土 × 地下水位が高いため、補助工法（薬液注入）を必須扱いとし工期 +2日（学習済みルール）"
        : "地下水位が管底より高い可能性があるため、補助工法の要否を設計者へ確認",
    );
  }
  if (overLength) {
    if (fit === "◎") fit = "○";
    confidence -= 6;
    cautions.push("延長 60m 超のため、推力計算と中間ジャッキ要否を積算担当が確認");
    references.push({ doc: "参考資料（仕様書）", page: "p.9", label: "最大推進延長" });
  }
  if (handwritten) {
    confidence -= 8;
    cautions.push("手書き図面のため、管底高・立坑位置の読み取り精度に注意（要目視確認）");
  }
  if (unknownSoil) {
    fit = "○";
    confidence = Math.min(confidence, 62);
    rationale.push("土質データ未取得のため、標準土質（砂質土 区分B）を仮定した暫定判定");
    cautions.push("土質調査結果の提出を依頼し、確定判定は再度実施");
  }
  if (rationale.length === 1) rationale.push("標準的な適用条件のため、積算資料の標準歩掛をそのまま適用");

  const estimate = Math.round((u.base + u.perM * input.length + (soft && highWater ? 420_000 : 0)) / 10_000) * 10_000;

  return {
    machine: deviationFromSpec ? `${machine}（φ200 へ変更提案）` : machine,
    fit,
    confidence: Math.max(50, confidence),
    estimate,
    dailyAdvance: advance,
    durationDays,
    rationale,
    cautions,
    references,
    deviationFromSpec,
  };
}

export function buildInquiry(input: NewInquiryInput, existing: Inquiry[]): Inquiry {
  const now = new Date();
  const ymd = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, "0")}${String(now.getDate()).padStart(2, "0")}`;
  const seq = existing.filter((q) => q.id.startsWith(`INQ-${ymd}`)).length + 1;
  const id = `INQ-${ymd}-${String(seq).padStart(2, "0")}`;
  const kind = input.workType.includes("取付") ? "取付管" : input.workType.includes("事前") ? "事前相談" : "本管新設";
  const title = `${kind} φ${input.pipeDiameter} 推進延長 ${input.length}m（${input.soil.split("（")[0]}）`;
  return {
    id,
    receivedAt: now.toISOString(),
    client: input.client,
    clientType: input.clientType,
    title,
    workType: input.workType,
    pipeDiameter: input.pipeDiameter,
    length: input.length,
    soil: input.soil,
    nValue: input.nValue,
    groundwater: input.groundwater,
    shaft: input.shaft,
    source: input.source,
    status: "pending_review",
    proposal: generateProposal(input),
  };
}
```

## 改訂履歴

| 版 | 日付 | 内容 |
| --- | --- | --- |
| v1.0 | 2026-09-12 | 初版作成（モック実装 v1 と同期）。同日、操作ガイド（`/guide`）、新規案件の登録（`/inquiries/new`）、参照データ管理（`/sources`）を追加 |
